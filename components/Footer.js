import styles from '../styles/Footer.module.css';

const Footer = () => (
  <div className={styles.wrapper}>
    <div>Driven by React + Node + AntD </div>
    <div>JZblog.com</div>
  </div>
);

export default Footer;
